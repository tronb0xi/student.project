Frontend placeholder.

Move your templates from `core/templates` and `users/templates` here. For now the Django project still serves templates from apps; to use this folder, update `config/settings.py` TEMPLATES['DIRS'] to include BASE_DIR / 'frontend' / 'templates'.
